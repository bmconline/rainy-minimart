# Server
PORT=3001
NODE_ENV=production

# Database
DATABASE_PATH=./rainy.db
# สำหรับ PostgreSQL (ต่อไปนี้):
# DATABASE_URL=postgresql://user:password@host:port/database

# CORS
CORS_ORIGIN=http://localhost:3000
# Production: CORS_ORIGIN=https://your-domain.com
